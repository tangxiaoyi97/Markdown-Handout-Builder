# Edition archive

Rendered PDFs preserved per edition. `dist/` is disposable build output;
this folder is the durable record.

| File | Edition | Provenance |
|:--|:--|:--|
| `obsidian-syntax-showcase-1.0-dialect.pdf` | 1.0-dialect | Re-rendered from the exact committed source state (`6560c8d`) after the original artifact was cleaned from `dist/` |
| `obsidian-syntax-showcase-2.0-dialect.pdf` | 2.0-dialect | Rendered at `522c0a2` (dialect-v2) |
| `dialect-guide-2.0-dialect.pdf` | 2.0-dialect | Rendered at `522c0a2` (dialect-v2) |

Note: PDFs are sizable binaries. If the repository should stay lean, attach
these to a GitHub Release instead of tracking them in git — the files here
are exactly what a release asset would contain.
